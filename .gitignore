# Dependencies
node_modules/

# Build output
out/
dist/
.vite/
*.tsbuildinfo

# Packaged installers (electron-builder output — never commit build artifacts)
release/
release-new/
release-build/
release-*/

# Tests
coverage/

# Logs
*.log

# Environment / local config
.env
.env.*
*.local

# OS cruft
.DS_Store
Thumbs.db

# Marketing site (deployed separately, not part of the app)
website/

# Relay build artifact (npm run build:relay)
relay/relay.mjs
relay/relay.mjs.map
# Relay runtime token-hash store
relay/relay-store.json
relay-store.json
